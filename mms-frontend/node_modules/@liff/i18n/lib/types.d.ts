import { navigatorLanguagesList } from './consts';
import XLTKeys from './xlt-types';
declare type ElementType<T extends ReadonlyArray<unknown>> = T extends ReadonlyArray<infer ElementType> ? ElementType : never;
export declare type Languages = ElementType<typeof navigatorLanguagesList>;
export declare type Keys = ElementType<typeof XLTKeys>;
export declare type XLTData = {
    [key in Keys]: string;
};
export interface Manifest {
    createdAt: number;
    languages: {
        [key in Languages]?: string;
    };
}
export {};
