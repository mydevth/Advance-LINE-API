import type { LiffInstance } from './types';
export interface ScanCodeResult {
    value: string | null;
}
export declare type ScanCode = () => Promise<ScanCodeResult>;
export declare function createScanCode(liff: LiffInstance): ScanCode;
