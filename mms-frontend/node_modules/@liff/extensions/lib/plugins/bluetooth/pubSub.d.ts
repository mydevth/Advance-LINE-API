export default pubSub;
declare namespace pubSub {
    function publish(key: any): void;
    function publish(key: any): void;
    function subscribe(key: any, callback: any): void;
    function subscribe(key: any, callback: any): void;
}
