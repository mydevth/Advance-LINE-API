import { LiffMenuColorSetting } from '@liff/types';
export interface LiffColors extends LiffMenuColorSetting {
    lightButtonBorderColor: string;
    baseBackgroundColor: string;
    baseTextColor: string;
}
