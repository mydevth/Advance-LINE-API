import { openWindow } from '@liff/open-window';
import { isInClient } from '@liff/is-in-client';
import { call } from '@liff/native-bridge';
import type { LiffInstance as _LiffInstance } from '@liff/check-availability/lib/types';
export interface LiffInstance extends _LiffInstance {
    id: string;
    openWindow: typeof openWindow;
    isInClient: typeof isInClient;
    _call: typeof call;
}
