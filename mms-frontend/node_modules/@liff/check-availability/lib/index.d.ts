import type { LiffInstance } from './types';
declare type LiffInstanceOld = LiffInstance & {
    checkFeature(apiName: string): boolean;
};
export declare class CheckAvailability<T extends LiffInstance | LiffInstanceOld> {
    private static get SDK_VERSION_SUPPORTING_NEW();
    private impl;
    constructor(liff: T);
    invoke(apiName: string): boolean;
}
export {};
